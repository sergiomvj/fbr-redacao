# Variáveis do servidor Next.js (SEM prefixo NEXT_PUBLIC_ — não vaza ao browser)

# Supabase — usadas APENAS em Server Components / Route Handlers
SUPABASE_URL=
SUPABASE_ANON_KEY=

# Iron Session
SESSION_SECRET=<minimo_32_caracteres_aqui>
SESSION_COOKIE_NAME=fbr_redacao_session

# URL da API do Backend (FastAPI)
BACKEND_URL=http://backend:8000

# WebSocket do Backend (para hooks de presença)
# Único NEXT_PUBLIC_ permitido — necessário para WebSocket no browser
NEXT_PUBLIC_WS_URL=ws://localhost:8000
