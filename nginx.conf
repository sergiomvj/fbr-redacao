# Basic nginx placeholder — Easypanel gerencia o reverse proxy e SSL automaticamente.
# Este arquivo é mantido apenas para compatibilidade local com docker-compose.
events {}
http {
    server {
        listen 80;
    }
}
